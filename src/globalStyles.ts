import { css } from "styled-components";
import theme from "./theme";

export default css`
  html,
  body {
    padding: 0;
    margin: 0;
    font-size: 62.5%;
    font-family: ${theme.fontPrimary};
  }
`;
